#include<iostream>
using namespace std;
int main(){
    //Logical Operator
/*
1. && (And)
2. || (Or)
3. ! (Not)
*/
int a,b;
a = 0 && 0;  //if there is no comparision sign , this is (a = 0 && 0) called expression
cout<<a <<endl;
a = 5 && -56;  // here we check a is 0 or non zero if a both value is non zero it return 1 if one of them is 0 it return 0;
cout<<a <<endl;
b =45 && 0;
cout<<!b;

//Relational Operator
/*
1.
}
